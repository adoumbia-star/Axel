# Copié automatiquement par l'intégration Neon du Marketplace Vercel,
# ou à renseigner depuis Neon > Connection Details.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Identifie les événements écrits par le prototype.
APP_ENV=development
